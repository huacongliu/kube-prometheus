kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-prome-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
  storageClassName: nfs-client-prometheus
